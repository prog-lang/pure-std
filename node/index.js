const { id, first, second, flip, compose } = require("./core");
const maybe = require("./maybe");
const io = require("./io");
const str = require("./str");
const net = require("./net");

module.exports = {
  id,
  first,
  second,
  flip,
  compose,
  maybe,
  io,
  str,
  net,
};
